// A $( document ).ready() block.
$(document).ready(function () {
    $("#currentDay").textContent = (moment().format('ddd MMM Do, YYYY'));
});


//targetcoloroftext = yourarray(math.floor(math.random() * yourarray.length));
//$("$#yourid").css("color", tragetcoloroftext());

    // Array holds all of the drinks available
    // var hourList = [
    //     9, 10, 11, 12, 1, 2, 3, 4, 5
    // ];
// possible sort array: yourarray.sort( function(){
  //  Return 0.5-Math.random();
//});
    // This line of jQuery selects the div with the matching id (#drink-options)
    // var hourDiv = $(".container");

    // Here we loop through our array using the .each() method and append a new div with each iteration
    // $.each(hourList, function (i, hour) {
    //     hourDiv.append("<div>" + hour + "</div>");
    // });


















